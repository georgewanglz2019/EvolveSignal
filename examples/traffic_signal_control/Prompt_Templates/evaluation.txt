Evaluate the following Python function designed for optimizing signal timing at a fixed-time, four-phase intersection.
Score the code on a scale from 0.0 to 1.0 for the following task-specific performance metrics:
1. Delay Score: How well does the signal plan generated by the code minimize average delay across all scenarios?
2. Stops Score: How effectively does the algorithm reduce the average number of stops per vehicle?
3. Combined Score: A balanced average of delay and stops performance, calculated as:
combined_score = 0.5 * delay_score + 0.5 * stops_score
where:
delay_score = 1.0 / (1.0 + avg_delay / 100.0)
stops_score = 1.0 / (1.0 + avg_stops / 1.0)

For each metric, provide a score between 0.0 and 1.0, where 1.0 is best. Base your evaluation on the code's likely behavior, logic correctness, and its ability to minimize these metrics across diverse traffic demand scenarios.

Code to evaluate:
```python
{current_program}
```
Return your evaluation as a JSON object, with the reasoning placed first, followed by the individual scores.:
{
  "reasoning": "[brief explanation of why the code performs this way on the three metrics]",
  "delay_score": [score],
  "stops_score": [score],
  "combined_score": [score]
}